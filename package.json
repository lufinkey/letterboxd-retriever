{
	"name": "letterboxd-retriever",
	"version": "1.1.0",
	"description": "An unofficial library for retrieving data from letterboxd",
	"main": "dist/lib/index.js",
	"types": "dist/lib/index.d.ts",
	"repository": {
		"type": "git",
		"url": "https://github.com/lufinkey/letterboxd-retriever.git"
	},
	"scripts": {
		"test": "npm run build && node dist/lib/test.js",
		"build": "tsc",
		"lint": "eslint . --ext .ts",
		"prepare": "npm run build"
	},
	"keywords": [
		"letterboxd"
	],
	"author": "Luis Finke (luisfinke@gmail.com)",
	"license": "ISC",
	"dependencies": {
		"cheerio": "^1.0.0"
	},
	"devDependencies": {
		"@types/node": "^22.1.0",
		"typescript": "^5.5.4"
	}
}
